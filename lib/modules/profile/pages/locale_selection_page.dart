import 'package:flutter/material.dart';

class LocaleSelectionPage extends StatelessWidget {
  const LocaleSelectionPage();

  static const routeName = '/localeSelectionPage';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
