const kWebsiteUrl = 'https://news-prod-1.web.app';

/// The minimum width at which navigation menu items are moved from the bottom
/// navigation bar to the drawer (sidebar).
const kMinDesktopWidth = 961;
